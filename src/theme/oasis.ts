import { createMuiTheme } from "@material-ui/core/styles";

export const oasisTheme = () =>
  createMuiTheme({
    palette: {
      primary: {
        main: "#185ADB",
      },
      secondary: {
        main: "#FFC947",
      },
    }
  });
